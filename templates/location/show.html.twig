{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodeLocation</th>
                <td>{{ location.codeLocation }}</td>
            </tr>
            <tr>
                <th>DateDebutLocation</th>
                <td>{{ location.dateDebutLocation ? location.dateDebutLocation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinLocation</th>
                <td>{{ location.dateFinLocation ? location.dateFinLocation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_location_index') }}" class="btn-link">Retour à la liste</a>

    <a href="{{ path('app_location_edit', {'codeLocation': location.codeLocation}) }}"class="btn-link">Modifier</a>

    {{ include('location/_delete_form.html.twig') }}
{% endblock %}
