{% extends 'base.html.twig' %}

{% block title %}Détails de la Voiture{% endblock %}

{% block body %}
    <h1>Détails de la Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Immatriculation</th>
                <td>{{ voiture.immatriculation }}</td>
            </tr>
            <tr>
                <th>Modèle</th>
                <td>{{ voiture.modele }}</td>
            </tr>
            <tr>
                <th>Prix Abidjan</th>
                <td>{{ voiture.prixAbidjan }}</td> <!-- Affiche le prix pour Abidjan -->
            </tr>
            <tr>
                <th>Prix Hors Abidjan</th>
                <td>{{ voiture.prixHorsAbidjan }}</td> <!-- Affiche le prix hors Abidjan -->
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque.libelleMarque }}</td> <!-- Affiche la marque -->
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_voiture_index') }}" class="btn-link">Retour à la liste</a>

    <a href="{{ path('app_voiture_edit', {'immatriculation': voiture.immatriculation}) }}" class="btn-link">Modifier</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
