{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodeClient</th>
                <td>{{ client.codeClient }}</td>
            </tr>
            <tr>
                <th>NomClient</th>
                <td>{{ client.nomClient }}</td>
            </tr>
            <tr>
                <th>PrenomClient</th>
                <td>{{ client.prenomClient }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_client_index') }}" class="btn-link">Retour à la liste </a>

    <a href="{{ path('app_client_edit', {'codeClient': client.codeClient}) }}" class="btn-link">Modifier</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
